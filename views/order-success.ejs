<%- include('partials/head') %>
<%- include('partials/nav') %>
<main id="main-content" class="success-page"><section class="success-card"><div class="success-mark">✓</div><p class="eyebrow">PEDIDO CRIADO</p><h1>DROP <em>RESERVADO.</em></h1><p>Seu pedido <strong><%= order.order_number %></strong> foi registrado. A confirmação do pagamento libera a separação.</p><div class="success-order-summary"><span>Total inicial</span><strong><%= formatCurrency(order.total_cents) %></strong></div><% if (order.payment_method === 'pix') { %><div class="pix-box"><p class="eyebrow">PAGAMENTO PIX</p><strong><%= settings.pix_beneficiary || 'NEBLK' %></strong><code><%= settings.pix_key || 'Configure a chave Pix no painel' %></code><p><%= settings.pix_instructions || 'Envie o comprovante para validação.' %></p></div><% } else { %><div class="pix-box"><p class="eyebrow">ATENDIMENTO</p><p>A NEBLK entrará em contato para combinar o pagamento e o frete.</p></div><% } %><div class="success-actions"><a href="/conta/pedidos/<%= order.order_number %>" class="button button-primary">VER PEDIDO</a><a href="/loja" class="text-link">VOLTAR PARA A LOJA</a></div></section></main>
<%- include('partials/footer') %>
