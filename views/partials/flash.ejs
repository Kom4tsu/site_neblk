<% if (flash) { %>
  <div class="flash flash-<%= flash.type %>" role="status"><span><%= flash.text %></span><button type="button" aria-label="Fechar mensagem" data-dismiss-flash>×</button></div>
<% } %>
